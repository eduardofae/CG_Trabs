#pragma once

enum rType {
    point    = 0,
    line     = 1,
    triangle = 2
};

enum orders {
    cw  = 0,
    ccw = 1
};

enum cStyle {
    camLookAt = 0,
    camFree   = 1
};

enum pType {
    perspective = 0,
    ortographic = 1
};